import react from "react";
import {View, Text, StyleSheet} from 'react-native';

export default function QuestList({style, location, title, content}) {
    
    return (
        
        <view style={internal}>
            
        </view>
    );
};

const internalStyles = StyleSheet.create({



});